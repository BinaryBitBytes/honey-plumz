const counting = (count) => {
    var counted = () => {
    for (let count=0; count<0; count++){ 
        count = 31;
        console.log(count);
    
        return 
    }
    console.log(counted);
    return 
    }
   counting.counted();
    console.log(counted)
}
export default counting;