const counting = (count) => {
    const count = () => {
    for(let count=0; count<=0; count++){ 
        count = 31;
        console.log(count(31));
    }
    return
    }
}
counting(31);
export default counting;